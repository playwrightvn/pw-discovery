import { test } from './008-fixture';

test('Login success1 - 01', async ({ myLoginFixture }) => {
    console.log('Login success - 01; using myFixture')
})

test('Login success - 02', async ({ myLoginFixture }) => {
    console.log('Login success - 02; using myFixture')
})