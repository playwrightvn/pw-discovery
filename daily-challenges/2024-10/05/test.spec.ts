import { test, expect } from '@playwright/test'

test('test run guardrails', async ({ page }) => {
            console.log('this code is not formatted');
            console.log('password is: abc123');
})
