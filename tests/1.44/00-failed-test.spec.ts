import { test, expect } from '@playwright/test';

test('Test 01', async( { }) => {
  expect(0).toEqual(1);
})

test('Test 02', async( { }) => {
 expect(1).toEqual(1);
})

test('Test 03', async( { }) => {
  expect(1).toEqual(1);
 })